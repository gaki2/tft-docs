import { describe } from 'vitest';

describe('MarkDown', () => {
  it('should render', () => {
    expect(true).toBe(true);
  });

  it('create popup when press `/`', () => {
    expect(true).toBe(true);
  });

  it('should render', () => {
    expect(true).toBe(true);
  });
});
